# WebElement'in görünürlüğünü değiştiren fonksiyon
# private static void setElementVisible(WebElement element, WebDriver driver) {
# JavascriptExecutor jsExecutor = (JavascriptExecutor) driver;
#jsExecutor.executeScript("arguments[0].style.opacity='1';", element);


